[phases.setup]
nixPkgs = [
  "python312",
  "python312Packages.pip",
  "gcc",
  "glib",
  "pango",
  "cairo",
  "gdk-pixbuf",
  "libffi",
  "shared-mime-info",
  "wkhtmltopdf"
]

[phases.install]
cmds = [
  "python -m pip install -r requirements.txt",
  "python -m playwright install chromium"
]

[phases.start]
cmd = "python -m flask --app app db upgrade && python -m gunicorn app:app --bind 0.0.0.0:$PORT"
